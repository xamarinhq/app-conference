<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
     xmlns:local="clr-namespace:Conference.Clients.UI;assembly=Conference.Clients.UI"
    x:Class="Conference.Clients.UI.FilterSessionsPage"
    Title="Filtros de sesiones"
    
    BackgroundColor="{DynamicResource WindowBackgroundTable}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Done" x:Name="ToolbarDone"/>
    </ContentPage.ToolbarItems>
    <TableView Intent="Settings" HasUnevenRows="true">
        <TableSection Title="Filters" x:Name="TableSectionFilters">
           <!-- <local:CategoryCell x:Name="ShowPastSwitch"/>
            <local:CategoryCell x:Name="ShowFavoritesOnlySwitch"/>-->
        </TableSection>
        <TableSection Title="Categories" x:Name="TableSectionCategories">
            <!--These are dynamically added-->
        </TableSection>
    </TableView>
</ContentPage>
