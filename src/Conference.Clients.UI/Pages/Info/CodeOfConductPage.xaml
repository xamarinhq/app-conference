<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="Conference.Clients.UI.CodeOfConductPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Conference.Clients.UI;assembly=Conference.Clients.UI"
    Title="Código de Conducta"
    BackgroundColor="{DynamicResource WindowBackgroundTable}">
    <local:AlwaysScrollView>
        <StackLayout Padding="{DynamicResource CardPadding}" Spacing="{DynamicResource CardSpacing}">
            <local:CardView>
                <StackLayout Spacing="0">
                    <StackLayout
                        Padding="{StaticResource StandardPadding}"
                        BackgroundColor="White"
                        HorizontalOptions="FillAndExpand"
                        VerticalOptions="FillAndExpand">

                        <Label x:Name="CodeOfConductText" Style="{DynamicResource ConferenceListItemDetailextStyle}" />
                    </StackLayout>
                </StackLayout>
            </local:CardView>
        </StackLayout>
    </local:AlwaysScrollView>
</ContentPage>
